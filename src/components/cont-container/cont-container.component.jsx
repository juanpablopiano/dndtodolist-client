import React from "react";
import "./cont-container.styles.scss";

const ContainerContainer = props => (
	<div className="container-container">{props.children}</div>
);

export default ContainerContainer;
